# 📦 Система управления багажом - Инструкция по запуску

Курсовая работа по теме №96: Разработка ПО для создания и обработки сведений о багаже пассажиров.

---

## 🚀 Быстрый старт (для друга на Linux)

### 1. Установи Docker (если еще не установлен)

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose

# Добавь себя в группу docker (чтобы не писать sudo)
sudo usermod -aG docker $USER

# ВАЖНО: Перезайди в систему после этого!
```

### 2. Скачай проект

```bash
# Если у тебя есть архив:
tar -xzf baggage_system.tar.gz
cd baggage_system

# Или через git:
git clone <repository_url>
cd baggage_system
```

### 3. Запусти приложение

```bash
# Дай права на выполнение скрипта
chmod +x run.sh

# Запусти
./run.sh
```

**Готово!** Приложение откроется в отдельном окне.

---

## 📋 Вход в систему

**Администратор:**
- Логин: `admin`
- Пароль: `admin`

**Гостевой режим:**
- Нажми кнопку "Гостевой режим" (только просмотр)

---

## 🐛 Что делать если не работает?

### Проблема: "Cannot connect to X server"

**Решение 1:** Разреши доступ к X11
```bash
xhost +local:docker
```

**Решение 2:** Если Wayland (не X11)
```bash
# Проверь:
echo $XDG_SESSION_TYPE

# Если wayland, переключись на X11 или используй:
export QT_QPA_PLATFORM=wayland
```

### Проблема: "Permission denied" при запуске Docker

**Решение:**
```bash
# Добавь себя в группу docker
sudo usermod -aG docker $USER

# Перезайди в систему (logout/login)
# Или перезагрузись
```

### Проблема: Порт 5432 занят

**Решение:**
```bash
# Останови локальный PostgreSQL
sudo systemctl stop postgresql

# Или измени порт в docker-compose.yml (раскомментируй и измени)
```

---

## 📁 Структура проекта

```
baggage_system/
├── src/                    # Исходный код C++
├── include/                # Заголовочные файлы
├── resources/              # Стили и ресурсы Qt
├── docker-compose.yml      # Конфигурация Docker
├── Dockerfile              # Образ приложения
├── init-db.sql            # Инициализация БД
├── CMakeLists.txt         # Сборка проекта
├── run.sh                 # Скрипт запуска ⭐
└── README_SETUP.md        # Эта инструкция
```

---

## 🎓 Функции приложения (по ТЗ)

1. ✅ Создать файл базы данных
2. ✅ Вывод содержимого на экран
3. ✅ Фильтр пассажиров (1 вещь, 20-30 кг)
4. ✅ Создание файла сводки
5. ✅ Просмотр файла сводки
6. ✅ Добавление записи
7. ✅ Удаление по номерам рейсов
8. ✅ Изменение количества вещей

---

## 🔐 Безопасность

- ✅ PostgreSQL доступна только внутри Docker network
- ✅ Ограничения ресурсов для контейнеров
- ✅ Транзакции для защиты данных
- ✅ Валидация всех входных данных

---

## 🛠️ Для разработчиков

### Локальная сборка (без Docker)

```bash
# Установи зависимости
sudo apt install qt6-base-dev libqt6sql6-psql cmake build-essential

# Собери проект
mkdir -p build && cd build
cmake ..
cmake --build .

# Запусти PostgreSQL
docker-compose up -d postgres

# Запусти приложение
export DB_HOST=localhost DB_PORT=5432 DB_NAME=baggage_db DB_USER=postgres DB_PASSWORD=postgres
./BaggageSystem
```

### Остановка всех контейнеров

```bash
docker-compose down
```

### Полная очистка (удалить данные)

```bash
docker-compose down -v
```

---

## 📞 Контакты

По вопросам писать автору проекта.

**Версия:** 2.0
**Дата:** 2025
**ВУЗ:** Курсовая работа
