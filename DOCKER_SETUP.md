# 🚀 Инструкция по запуску через Docker

## Быстрый старт

### 1. Запустить PostgreSQL
```bash
cd baggage_system
docker-compose up -d postgres
```

### 2. Подождать инициализации (5-10 секунд)
```bash
sleep 8
```

### 3. Запустить приложение локально
```bash
./run-local.sh
```

## 🔑 Данные для входа

### Администратор (полный доступ):
- **Логин:** `admin`
- **Пароль:** `poprobuy_vzlomat15`

### Обычный пользователь:
Нажмите **"Регистрация"** и создайте свою учетную запись

### Гостевой режим (только чтение):
Нажмите **"Гостевой режим"**

---

## 📋 Что нового

### ✅ Безопасность
- ❌ **УДАЛЕНА** проблема с логированием логинов пользователей
- ✅ Новый безопасный пароль администратора
- ✅ Только один админ в системе
- ❌ **УДАЛЕН** тестовый пользователь test/test

### ✅ База данных (НОРМАЛИЗАЦИЯ)
- ✅ **Новая таблица `baggage_items`** - веса хранятся отдельно
- ✅ Проверки целостности на уровне БД (`CHECK` constraints)
- ✅ Каскадное удаление связанных записей
- ✅ Улучшенная производительность запросов

### ✅ Контроль доступа (RBAC)
**Администратор может:**
- ✅ Просматривать все записи
- ✅ Добавлять записи
- ✅ Изменять записи
- ✅ **Удалять записи**
- ✅ Создавать файлы

**Обычный пользователь может:**
- ✅ Просматривать все записи
- ✅ Добавлять записи
- ✅ Изменять записи
- ❌ **НЕ МОЖЕТ удалять** (только админ)
- ❌ **НЕ МОЖЕТ создавать файлы**

**Гость может:**
- ✅ Только просматривать записи
- ❌ Никакие изменения невозможны

---

## 🗄️ Структура БД

### Таблица `users`
```sql
id            SERIAL PRIMARY KEY
username      VARCHAR(100) UNIQUE
password_hash VARCHAR(64)        -- SHA-256 хеш
role          VARCHAR(20)        -- admin/user
created_at    TIMESTAMP
```

### Таблица `baggage_records`
```sql
id             SERIAL PRIMARY KEY
flight_number  VARCHAR(50)
passenger_name VARCHAR(255)
created_at     TIMESTAMP
updated_at     TIMESTAMP
created_by     INTEGER           -- ID пользователя
```

### Таблица `baggage_items` ⭐ НОВАЯ
```sql
id                SERIAL PRIMARY KEY
baggage_record_id INTEGER → baggage_records(id) ON DELETE CASCADE
item_number       INTEGER CHECK (1 <= item_number <= 5)
weight            NUMERIC(5,2) CHECK (0 < weight <= 100)
UNIQUE(baggage_record_id, item_number)
```

---

## 🛠️ Команды для работы

### Проверить статус PostgreSQL
```bash
docker-compose ps
```

### Остановить всё
```bash
docker-compose down
```

### Пересоздать БД с нуля
```bash
docker-compose down -v
docker-compose up -d postgres
sleep 8
```

### Проверить логи БД
```bash
docker-compose logs postgres | tail -30
```

### Подключиться к БД вручную
```bash
docker-compose exec postgres psql -U postgres -d baggage_db
```

---

## ✨ Исправленные проблемы

| Проблема | Статус | Решение |
|----------|--------|---------|
| Слабый пароль admin/admin | ✅ ИСПРАВЛЕНО | Новый пароль: poprobuy_vzlomat15 |
| Логирование логинов | ✅ ИСПРАВЛЕНО | Убраны username из логов |
| item_weights как TEXT | ✅ ИСПРАВЛЕНО | Нормализация: таблица baggage_items |
| Нет контроля доступа | ✅ ИСПРАВЛЕНО | RBAC на основе ролей |
| Тестовый пользователь | ✅ УДАЛЕН | Только admin |

---

## 📝 Примечания

- БД автоматически инициализируется при первом запуске
- Тестовые данные загружаются автоматически (5 записей багажа)
- PostgreSQL работает на порту 5432
- Приложение подключается к localhost:5432

---

## ❗ Важно

1. **НЕ коммитьте файл .env в git** (содержит пароли)
2. Пароль `poprobuy_vzlomat15` - только для разработки
3. В продакшене используйте более сложные пароли
4. Регулярно делайте бэкапы БД

---

✅ **Готово к использованию!**
