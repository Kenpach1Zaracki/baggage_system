# 📦 Система управления багажом - Инструкция для запуска

## 🚀 КАК ЗАПУСТИТЬ (2 команды!)

### Шаг 1: Разрешить Docker использовать экран
```bash
xhost +local:docker
```

### Шаг 2: Запустить приложение
```bash
docker-compose up --build
```

**ВСЁ!** Окно приложения откроется автоматически.

---

## 🔑 Войти в систему

### Вариант 1: Администратор (полный доступ)
- **Логин:** `admin`
- **Пароль:** `poprobuy_vzlomat15`

### Вариант 2: Зарегистрировать нового пользователя
Нажмите кнопку **"Регистрация"**

### Вариант 3: Гостевой режим (только смотреть)
Нажмите кнопку **"Гостевой режим"**

---

## ⚙️ Требования

- **Linux** с установленным **Docker** и **Docker Compose**
- Работающий X11 (обычный графический режим)

---

## 🛑 Остановить приложение

Просто нажмите **Ctrl+C** в терминале, где запущен docker-compose

---

## ❓ Что-то не работает?

### Ошибка "cannot open display"
```bash
export DISPLAY=:0
xhost +local:docker
docker-compose up --build
```

### Ничего не происходит
```bash
# Остановить всё
docker-compose down

# Полностью пересоздать
docker-compose down -v
docker-compose up --build
```

---

## 📁 Что внутри

- **PostgreSQL** база данных (автоматически настраивается)
- **Qt 6** приложение с графическим интерфейсом
- Всё запаковано в **Docker контейнеры**
- **Ничего не нужно устанавливать** на компьютер!

---

## ✅ Проверка что всё работает

После команды `docker-compose up --build` должно быть:

1. ⏳ Идёт сборка приложения (~30 секунд)
2. 🐘 PostgreSQL запустился
3. ✅ Сообщение "✓ Подключение к PostgreSQL успешно"
4. 🖥️ **Окно входа в систему появилось на экране**

---

**🎉 ГОТОВО! ПРОСТО ДВЕ КОМАНДЫ!**

---

## 🎯 Что умеет приложение

- ✅ Добавлять записи о багаже пассажиров
- ✅ Искать багаж по номеру рейса или пассажиру
- ✅ Изменять данные о багаже
- ✅ Удалять записи (только администратор)
- ✅ Создавать отчёты по датам
- ✅ Сохранять отчёты в файлы
- ✅ Роль-based контроль доступа (админ/пользователь/гость)

---

**Если что-то непонятно - спроси того, кто тебе это отправил!**
