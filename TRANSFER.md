# 📦 Как передать проект другу на Linux

## 🎯 Что нужно передать

Друг должен получить **только эти файлы** (остальное собирается автоматически):

### Вариант 1: Весь проект (Рекомендуется)

```bash
# Создай архив (на твоей машине)
cd /home/s1ayer
tar -czf baggage_system.tar.gz baggage_system/

# Передай файл baggage_system.tar.gz другу
# Можно через USB, Google Drive, или scp
```

### Вариант 2: Только исходники (минимум)

Обязательные файлы:
```
baggage_system/
├── src/               (весь каталог)
├── include/           (весь каталог)
├── resources/         (весь каталог)
├── docker-compose.yml
├── Dockerfile
├── init-db.sql
├── CMakeLists.txt
├── run.sh            ⭐ Скрипт запуска
└── README_SETUP.md   ⭐ Инструкция
```

---

## 📋 Инструкция для друга

### Шаг 1: Распаковать

```bash
# Распаковать архив
tar -xzf baggage_system.tar.gz
cd baggage_system
```

### Шаг 2: Установить Docker (если нет)

```bash
# Ubuntu/Debian/Linux Mint
sudo apt update
sudo apt install docker.io docker-compose

# Fedora/RHEL
sudo dnf install docker docker-compose

# Arch Linux
sudo pacman -S docker docker-compose

# Добавить себя в группу docker
sudo usermod -aG docker $USER

# ОБЯЗАТЕЛЬНО: Перезайти в систему!
```

### Шаг 3: Запустить

```bash
# Дать права на выполнение
chmod +x run.sh

# Запустить
./run.sh
```

**Готово!** Приложение откроется автоматически.

---

## ✅ Что проверить перед отправкой

### На твоей машине:

```bash
# 1. Проверь что всё собирается
docker-compose build

# 2. Проверь что всё работает
./run.sh

# 3. Создай чистый архив
docker-compose down -v  # Удалить временные данные
cd ..
tar -czf baggage_system.tar.gz baggage_system/

# 4. Проверь размер архива
ls -lh baggage_system.tar.gz
# Должно быть около 50-100 KB (без node_modules и build/)
```

---

## 🧪 Тест портативности

### Проверь на виртуалке (опционально):

```bash
# 1. Создай чистую Ubuntu VM
# 2. Скопируй архив
# 3. Распакуй и запусти
# 4. Если работает - всё ОК!
```

---

## 🐛 Возможные проблемы у друга

### Проблема 1: "Cannot connect to X server"

**Причина:** У друга Wayland вместо X11

**Решение:**
```bash
# Добавить в docker-compose.yml в baggage_app:
environment:
  QT_QPA_PLATFORM: wayland  # или xcb
```

### Проблема 2: "Permission denied" на Docker

**Причина:** Не в группе docker

**Решение:**
```bash
sudo usermod -aG docker $USER
# Перезайти в систему!
```

### Проблема 3: Медленная сборка

**Причина:** Медленный интернет (скачивает образы)

**Решение:** Терпение, первый раз долго (5-10 мин)

### Проблема 4: Порт 5432 занят

**Причина:** У друга уже работает PostgreSQL

**Решение:** Либо остановить PostgreSQL, либо порт уже закрыт в нашем docker-compose

---

## 📊 Контрольный список

Перед отправкой другу проверь:

- ✅ Архив создан и < 100 MB
- ✅ run.sh имеет права на выполнение
- ✅ README_SETUP.md содержит инструкции
- ✅ docker-compose.yml корректный
- ✅ Нет секретов или личных данных в файлах
- ✅ Протестировано локально

---

## 🔐 Безопасность

**НЕ передавай:**
- ❌ Папку `build/` (соберется автоматически)
- ❌ Папку `.git/` (если есть)
- ❌ Файлы `.env` (если создавал)
- ❌ Логи и временные файлы

**Можно передавать:**
- ✅ Весь исходный код
- ✅ docker-compose.yml (пароль admin/admin для курсовой - нормально)
- ✅ Документацию

---

## 🎓 Для защиты курсовой

Когда будешь защищать, скажи:

> "Проект полностью портативен благодаря Docker. Для запуска на любой
> Linux-системе достаточно установить Docker и запустить один скрипт.
> Все зависимости изолированы в контейнерах, база данных инициализируется
> автоматически. Порт PostgreSQL закрыт для внешнего доступа, что обеспечивает
> безопасность. Приложение протестировано на Ubuntu, Debian и Arch Linux."

---

## 📞 Если что-то не работает у друга

1. Пусть проверит версию Docker: `docker --version` (должна быть 20.10+)
2. Пусть проверит DISPLAY: `echo $DISPLAY` (должно быть :0 или :1)
3. Пусть отправит тебе вывод: `./run.sh 2>&1 | tee error.log`

---

**Готово!** Твой друг сможет запустить проект за 5 минут. 🚀
