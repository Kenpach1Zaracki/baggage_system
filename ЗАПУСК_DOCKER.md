# 🐳 Запуск через Docker (БЕЗ СКРИПТОВ)

## 🚀 БЫСТРЫЙ СТАРТ

### 1️⃣ Разрешить Docker доступ к X11 (выполнить ОДИН РАЗ)
```bash
xhost +local:docker
```

### 2️⃣ Запустить всё приложение
```bash
docker-compose up --build
```

**ВСЁ!** Приложение откроется автоматически.

---

## 🔑 Данные для входа

### Администратор (полный доступ):
- **Логин:** `admin`
- **Пароль:** `poprobuy_vzlomat15`

### Регистрация нового пользователя:
Нажмите кнопку **"Регистрация"** в окне входа

### Гостевой режим (только просмотр):
Нажмите кнопку **"Гостевой режим"**

---

## 📋 Команды

### Запустить (после первого раза, без пересборки)
```bash
docker-compose up
```

### Остановить
```bash
Ctrl+C
# или в отдельном терминале:
docker-compose down
```

### Пересоздать БД с нуля
```bash
docker-compose down -v
docker-compose up --build
```

### Посмотреть логи
```bash
docker-compose logs -f
```

### Посмотреть статус контейнеров
```bash
docker-compose ps
```

---

## ⚙️ Что происходит при запуске:

1. 🐘 **PostgreSQL** запускается в контейнере
2. 🗄️ **БД автоматически инициализируется** из `init-db.sql`:
   - Создаются таблицы `users`, `baggage_records`, `baggage_items`
   - Создаётся администратор (admin)
   - Загружаются 5 тестовых записей багажа
3. 🔨 **Qt приложение собирается** (multi-stage Docker build)
4. 🖥️ **GUI окно открывается** на вашем экране через X11

---

## ✅ Что уже реализовано

### Безопасность:
- ✅ Роль-based контроль доступа (RBAC)
- ✅ Удалены чувствительные данные из логов
- ✅ Только один администратор в системе
- ✅ Безопасный пароль (не admin/admin)

### База данных (нормализация):
- ✅ Таблица `baggage_items` для хранения весов
- ✅ CHECK constraints для валидации данных
- ✅ CASCADE DELETE для целостности
- ✅ Индексы для производительности

### Роли пользователей:
- **Администратор:** Полный доступ (просмотр, добавление, изменение, удаление, создание файлов)
- **Пользователь:** Ограниченный доступ (просмотр, добавление, изменение, НЕТ удаления)
- **Гость:** Только просмотр

---

## 🛠️ Решение проблем

### Ошибка "cannot open display"
```bash
xhost +local:docker
export DISPLAY=:0
docker-compose up
```

### Ошибка с правами X11
```bash
xhost +local:root
docker-compose up
```

### Приложение не открывается
```bash
# Проверить DISPLAY
echo $DISPLAY

# Если пусто или неправильно:
export DISPLAY=:0

# Перезапустить
docker-compose down
docker-compose up
```

### БД не инициализируется
```bash
# Полная очистка и пересоздание
docker-compose down -v
docker volume prune -f
docker-compose up --build
```

---

## 📦 Инструкция для одногруппника

**Отправьте ему:**
1. Всю папку `baggage_system/`
2. Скажите выполнить **ДВЕ команды**:
```bash
cd baggage_system
xhost +local:docker
docker-compose up --build
```

**Готово!** Никаких скриптов, никаких установок, ничего больше не нужно.

---

## 🎯 Преимущества Docker-запуска

✅ **НЕ нужно** устанавливать Qt 6
✅ **НЕ нужно** устанавливать PostgreSQL
✅ **НЕ нужно** устанавливать зависимости (libqt6, libxcb, etc.)
✅ **НЕ нужны** скрипты для запуска
✅ Работает на **любом Linux с Docker**
✅ **Две команды** для запуска: `xhost` + `docker-compose up`
✅ **Автоматическая инициализация** БД
✅ **Изолированная среда** (не засоряет систему)

---

## 📊 Проверка работы

После запуска `docker-compose up --build` вы должны увидеть:

1. Скачивание образов (если первый раз)
2. Сборку приложения (займёт ~20-30 секунд)
3. Запуск PostgreSQL (сообщение "ready to accept connections")
4. Запуск приложения (сообщение "✓ Подключение к PostgreSQL успешно")
5. **Окно авторизации на экране**

Войдите как `admin / poprobuy_vzlomat15` и работайте!

---

**🎉 ВСЕГО ДВЕ КОМАНДЫ - И ВСЁ РАБОТАЕТ!**
